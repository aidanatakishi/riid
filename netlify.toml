# Statik site. Giriş səhifəsi də yığılır; hesab yoxlaması Flask-dadır (python app.py).

[build]
  command = "python netlify/build.py"
  publish = "site"

[[redirects]]
  from = "/login"
  to = "/login.html"
  status = 200
